#include <stdio.h>

int main(void)
{
    int a  = 0;

    if (a < 0)
        printf("a < 0\n");
    else if (a == 0)
        printf("a == 0\n");
    else if (a >= 0)
        printf("a >= 0\n");
        
}
